Zuweisung: variablen_name = "Dies ist ein String"
						#eine Zuweisung erfolgt über den Zuweisungsoperator '='
						#der Variablentyp (String, Integer, Time, ...) erfolgt über die Eingabe

Zeichenkette (String):
		Ein String erzeugt, in dem eine beliebige Anzahl von Zeichen(Zahlen, Buchstaben)
		innerhalb von Anführungszeichen (sowohl 'einfache' als "doppelte") steht.

Zusammengesetzte Zeichenkette:
		tag = "Freitag"
		str = "Heute ist " + tag
			#das + Zeichen verbindet zwei Strings

Ganze Zahl (Integer, Fixnum):
		Zahl = 15

Float (Kommazahl)
		komma_zahl = 13.44

